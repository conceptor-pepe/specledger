# Specs

`docs/specs/` stores long-term system truth.

Recommended structure:

```text
docs/specs/
└── <domain>/
    ├── business.md
    ├── api.md
    ├── data.md
    └── invariants.md
```

Update long-term specs when a completed change modifies contracts, business rules, data semantics, or invariants.
