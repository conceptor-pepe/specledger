name: SpecLedger CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    name: Validate SpecLedger
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare Scripts
        shell: bash
        run: |
          set -euo pipefail
          chmod +x scripts/check.sh scripts/release-preflight.sh scripts/specledger-verify.sh scripts/specledger-archive.sh scripts/specledger-new-change.sh install/init.sh

      - name: Run SpecLedger checks
        shell: bash
        run: |
          set -euo pipefail
          ./scripts/check.sh
